#include "main.h"
#include <stdio.h>

/**
 * *_strcpy -   string pointed to by src
 * @dest: the pointer
 * @src:  string pointed
 * Return: \n
 */

 char *_strcpy(char *dest, char *src)
{
	int i = 0;

	for (i = 0; i < n; i++)
	{
	printf("%d", a[i]);
	if (i < n - 1)
	{
	printf(", ");
	}
}
	printf("\n");
}
