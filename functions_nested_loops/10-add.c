#include "main.h"
#include <stdlib.h>

/**
 * add - adds two integers and returns the result
 * @a: first number
 * @b: second number
 * Return:  the absolute value
 */

int add(int a, int b)

{
	int resul = a + b;

	return abs(resul);
}

