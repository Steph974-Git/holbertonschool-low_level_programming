#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 *  print whether the number stored in the variable n
 *
 * Retunr Alaways (Success)
/* 
int main(void)
{
	int n;
        if (n > 0)
{
             printf("%d is positive\n".n)
}
        else if (n == 0)
{
             printf("%d is zero\n".n)
}
        else (n < 0)
{
             printf("%d is negative\n".n)
}
	return (0);
}
