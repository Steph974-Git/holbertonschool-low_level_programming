#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Print  the number stored in the variable n
 *
 * Return: Always (Success)
 */

	int main(void)
{
	int number;

	for (number = 0 ; number < 10 ; number++)
{
	printf("%d", number);
}
	printf("\n");

	return (0);
}
